#include "location.hpp"
using namespace std;
Location::Location(string name, double latitude, double longitude, int traffic) : name(name), latitude(latitude), longitude(longitude), traffic(traffic)
{
}